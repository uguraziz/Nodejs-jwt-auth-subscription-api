# Uygulamanın çalışacağı port numarası
PORT = 5000

# MongoDB veritabanı bağlantı URL'si
DB_URL = "mongodb://localhost:27017/subscrible"

# JSON Web Token (JWT) için gizli anahtar
JWT_SECRET_KEY = ""
# JWT'nin geçerlilik süresi
JWT_EXPIRES_IN = "1d"

# Geçici JWT için gizli anahtar
JWT_TEMPORARY_SECRET_KEY = ""
# Geçici JWT'nin geçerlilik süresi
JWT_TEMPORARY_EXPIRES_IN = "30m"

# E-posta gönderme için kullanılacak hesabının kullanıcı adı
EMAIL_USER = ""
# E-posta gönderme için kullanılacak hesabının şifresi
EMAIL_PASSWORD = ""
